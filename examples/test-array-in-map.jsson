// Test arrays in map output
test [
  template { id, category }
  
  map (item) = {
    id = item.id
    category = item.category
    tags = ["tag1", "tag2", "tag3"]
    numbers = [1, 2, 3]
  }
  
  1, "electronics"
  2, "books"
]
