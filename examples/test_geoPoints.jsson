geoPoints [
  template { id, tag }

  map (g) = {
    id = "pt-" + g.id
    lat = -12.9000 + (g.id / 50) * 0.02
    lon = -38.3300 + (g.id % 50) * 0.02
    tag = g.tag
    type = "geo_point"
  }

  0..119, "zone-a"
]
