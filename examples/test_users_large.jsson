users [
  template { id }

  map (u) = {
    id = u.id
    user = "user" + u.id
    lat = -12.9 + (u.id / 100) * 0.02
    lon = -38.33 + (u.id % 100) * 0.02
    hash = "uid-" + (u.id * 91 % 17)
  }

  0..99
]
